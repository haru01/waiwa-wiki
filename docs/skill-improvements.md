# スキル改善バックログ

ドッグフーディングや運用の中で見つかった、`.claude/skills/` の改善余地を記録する。
各項目は「気づいた文脈／課題／改善案／対象スキル／状態」で書く。状態は `未対応` `対応中` `対応済み`。

> 注記: 以下で `/grill` と書かれているスキルは現在 `/formulate` に改称されている（当時の記録はそのまま残す）。

---

## SI-001: `/grill` が状況・行動仮説を引き出せていない

- **対象スキル**: `/grill`
- **状態**: 対応済み（`grill/SKILL.md` の手順2に順序と行動Q3、手順7に自己点検を追記。2026-07-16）
- **気づいた文脈**: 本ツール自体への `/grill` 適用（2026-07-16）。CPFで課題仮説（H-004〜H-007）は複数起票できたが、**状況・行動仮説は H-001 の1件のみ**で、しかも内容は「記録しようとしている」という状況寄りの記述に留まり、顧客が現在とっている**具体的な行動**（CPF問いかけバンクQ3）を独立した反証可能な仮説として引き出せなかった。
- **課題**: CPFは「状況・行動仮説」と「課題仮説」の2タイプを形成すべきなのに、対話が課題（痛み）へ早く流れてしまい、行動仮説が手薄になる。行動の事実を押さえないまま課題を語ると、後続の検証（過去の事実を聞く問題インタビュー）の土台が弱くなる。
- **改善案**:
  1. `/grill` の手順2に「まず状況・行動仮説を1本確定させてから課題仮説に進む」順序を明記する。
  2. 問いかけで、課題（Q4以降）に進む前に**行動を尋ねるQ3を必ず1問挟む**（「その状況で顧客は現在どんな行動をとっているか？」を具体的な動作レベルで）。
  3. セッション終了時に「状況・行動仮説が最低1件あるか」を自己点検するチェックを入れる（なければ行動の深掘りに戻す）。
- **関連**: CPF playbook（`playbooks/cpf.md`）の「形成すべき仮説タイプ」と整合させる。

---

## SI-002: `/plan` はテストカードと併せてインタビュースクリプト雛形を出せるとよい

- **対象スキル**: `/plan`（および `/ingest` との接続）
- **状態**: 対応済み（`plan/SKILL.md` 手順5にスクリプト草稿作成、`ingest/SKILL.md` 手順3に〈自認〉〈実コスト〉○×表を追記。2026-07-16）。ただし改善案3（`templates/demo-script.md`）は**未対応の残タスク**。
- **気づいた文脈**: ACT-001（問題インタビュー）のテストカードを作った直後、「方法」欄だけでは現場で実際に話す会話に落ちておらず、別途スクリプトが要ると気づいた（2026-07-16）。
- **課題**: テストカードは検証の設計（目的・指標・成功基準）は与えるが、**現場で使える会話スクリプト**を与えない。毎回ゼロから作ると、CPFの鉄則（過去の事実を聞く／ソリューションを見せない／誘導しない）が抜け落ちやすい。
- **改善案**:
  1. `/plan` の手順5で、interview/demo 系のテストカードを作ったら `templates/interview-script.md` を基に**対象仮説を差し込んだスクリプト草稿**も併せて提示する（活動レコードに添付、または別ファイルとしてリンク）。
  2. スクリプトに「対象者ごとの〈自認〉〈実コスト〉○×表」を組み込み、`/ingest` の成功基準判定（切実な課題トップ3）に直結させる。
  3. デモ用（PSF）の雛形も将来的に用意する（`templates/demo-script.md`）。
- **関連**: `templates/interview-script.md`、テストカード `wiki/activities/ACT-001.md`、`/ingest` の学習カード記入。

---

## SI-003: `/view list` の表に仮説どうしの関連（リンク）が出ていなかった

- **対象スキル**: `/view`（`list`）
- **状態**: 対応済み（ビュー改修済み＋ `view/SKILL.md` の `list`/`board` に派生元列・関連リンク列・mermaidチェーン図を明文化。2026-07-16）
- **気づいた文脈**: 一覧ビューを見たユーザーから「関連がわからない、リンクも表に含めて」との指摘（2026-07-16）。当初の `list` はID・確信度・ステータス等のスカラー項目だけで、派生元・解決関係・検証活動へのwikilinkを持たず、仮説どうしのつながり（系譜・課題↔解決策）が読み取れなかった。
- **課題**: 仮説検証Wikiの価値の一つは「探索ネットワークの可視化」なのに、一覧ビューがフラットな表で関係を落としていた。表とグラフ（Obsidian）を往復しないと関連が追えない。
- **改善案（実施済みの内容を仕様化する）**:
  1. `list` の全仮説テーブルに **「派生元」列** と **「関連リンク（解決／派生先／検証活動）」列** を必須にする（`[[H-NNN]]` `[[ACT-NNN]]` を明記）。
  2. 「行動→切実な課題→解決策→買ってもらえる」のバリューチェーンを **mermaid 図** で先頭に出す（ステージを貫く筋を一望）。
  3. `board`（ジャベリンボード）ビューでも同様に関連リンクを持たせる。
- **関連**: `wiki/views/hypotheses-list.md`（改修済み）、CLAUDE.md「Obsidian連携（探索ネットワークの可視化）」の意図と整合。

---

## SI-004: 会話から得た運用上の学びをスキルへ明文化（取り下げ／創発／Web調査／デモデータ）

- **対象スキル**: `/grill` `/ingest` `/lint`
- **状態**: 対応済み（2026-07-16）
- **気づいた文脈**: ドッグフーディング中に、SI-001〜003 以外にも規約化すべき運用が複数発生した（H-002/H-003 の取り下げ、`/ingest` 中の H-009/H-010 創発、デスクリサーチでの裏付け、架空データの扱い）。いずれもその場は手作業で処理したが、スキル定義に無いため再発時に判断がぶれる。
- **課題／改善（反映済みの内容）**:
  1. **取り下げ手順**（`/grill`）— 反証との区別／レコード削除／log追記／ID欠番の非再利用／被リンク修正。CLAUDE.md（スキーマ層）は変更せず運用で吸収。
  2. **取り込み中の仮説創発**（`/ingest` 手順3）— 事後発見の新仮説を起票し ACT に紐づけ、確信度は上げすぎない。
  3. **デスクリサーチでの裏付け**（`/ingest` 手順3）— `WebSearch` で三角測量し出典を残す。
  4. **架空/デモデータの明示**（`/ingest` 守ること）と、その**点検**（`/lint` 項目9）。
  5. **欠番の正常判定**（`/lint` 項目8）— log に取り下げ記録のある欠番は異常としない。
- **CLAUDE.md（スキーマ層）は今回保留**: status語に「取り下げ」を足す等の正式化は、人間合意のうえ別途検討する残タスク。
- **関連**: `.claude/skills/{grill,ingest,lint}/SKILL.md`、`wiki/log.md` の取り下げ記録（H-002/H-003）。



---

## SI-005: `id:` フロントマターの接頭辞規約が template/CLAUDE.md と実レコードで不一致

- **対象スキル**: `/grill` `/ingest` `/new-project`（および `templates/*.md`・`CLAUDE.md` スキーマ層）
- **状態**: 対応済み（`id` をファイル名と一致する接頭辞つきに統一。CLAUDE.md/`templates/*.md` の例・grill/plan/ingest/decide の作成手順・lint項目8を更新。self SELF-ACT-003/004 の id を修正。2026-07-17）
- **気づいた文脈**: スキル一巡テスト（2026-07-17・使い捨て `dogfood` 案件）。`templates/hypothesis.md` に従うと frontmatter は `id: H-NNN`（**接頭辞なし**）になり、実際 `DOG-H-001.md` の `id:` は `H-001` で生成された。一方 `projects/self/` の実レコードは `id: SELF-H-001`・`id: SELF-DEC-001` と**接頭辞つき**。CLAUDE.md のスキーマ例も `id: H-001`（接頭辞なし）。
- **課題**: ファイル名・wikilink は接頭辞つきで統一されているが、`id:` フィールドだけ「テンプレ/スキーマ=なし」「self実レコード=あり」で割れている。どちらが正か決まっていないため、案件ごとに `id:` の書き方がぶれる（grep や将来の集計スクリプトで揺れる）。
- **改善案**:
  1. `id:` を接頭辞つき（`SELF-H-001`）に統一するか、接頭辞なし（`H-001`）に統一するかを人間合意で決める。
  2. 決めた方に合わせて `templates/{hypothesis,activity,decision}.md`・`CLAUDE.md` のスキーマ例・各スキルの記述を揃える。
  3. `/lint` に「ファイル名と `id:` の接頭辞整合」チェックを追加する余地。
- **関連**: `CLAUDE.md`「レコード種別とスキーマ」、`projects/self/` 既存レコード、`templates/`。

---

## SI-006: stage-transition 以外の意思決定（persevere/pivot/kill/rollback）が孤立レコードになる

- **対象スキル**: `/decide` `/lint`
- **状態**: 対応済み（`index.md`（雛形・self）に「意思決定」節を新設し全DECを被参照。decide手順8で必須更新に。lint項目2でDECの孤立判定を明文化。2026-07-17）
- **気づいた文脈**: 一巡テストで `persevere` の意思決定 `DOG-DEC-001` を作ったところ、`/lint` 項目2（孤立レコード）に該当した。`stage-transition` の場合は `stage.md` のステージ履歴に `[[DEC-NNN]]` が載って被参照されるが（self の SELF-DEC-001 は stage.md・各ビューから参照済み）、stage を変えない `persevere`/`pivot`/`kill`/`rollback` は**どこからも wikilink されず**、グラフ上で孤立する。
- **課題**: 意思決定はDEC→ACT/Hへの**外向き**リンクは持つが、他レコードからの**被参照アンカー**が stage-transition 以外に用意されていない。lint 項目2が正当な意思決定を誤検出する（false positive）。
- **改善案**:
  1. `/decide` 手順に「影響を受けた仮説レコードの確信度履歴（または系譜節）に `[[DEC-NNN]]` を1件書き戻す」を追加し、全タイプの DEC を必ず被参照させる。
  2. あるいは意思決定インデックス（`wiki/decisions/index` か `index.md` の意思決定節）を設け、そこから全 DEC を参照する。
  3. `/lint` 項目2で「DEC は外向きリンクが正しければ孤立扱いしない」等の判定を明文化する。
- **関連**: `decide/SKILL.md` 手順5-6、`lint/SKILL.md` 項目2、`templates/decision.md`。

---

## SI-007: `/view` に HTMLダッシュボード（index.html）の再生成手順がない

- **対象スキル**: `/view`
- **状態**: 対応済み（`index.html` ヘッダの虚偽「生成物・再生成」を「手動スナップショット」に修正。view「守ること」と views README に「HTMLは任意・手動・/view対象外」を明記。生成器は作らない。2026-07-17）
- **気づいた文脈**: 一巡テストで `/view` の3サブコマンド（`vp`/`list`/`board`）を確認したが、`projects/self/wiki/views/index.html`（「self ダッシュボード」HTML・冒頭に「生成物。手編集禁止。list/board/vp から再生成する」と明記）は `/view` のどのサブコマンドにも対応していない。self には成果物として存在するのに、**再生成手順がスキルに定義されていない**。
- **課題**: 生成物（index.html）が SSoT からどう再生成されるかスキルに無いため、レコード更新後に HTML が陳腐化しても再生成の入口がない。`dogfood` 側では index.html が生成されず、案件間で views/ の構成が不揃いになる。
- **改善案**:
  1. `/view` に `html`（または `dashboard`）サブコマンドを追加し、list/board/vp の内容から `views/index.html` を再生成する手順を明記する。
  2. `/view` を引数なしで呼んだときに全ビュー（vp/list/board/html）を一括再生成する挙動も検討。
  3. 案件雛形（`templates/project/wiki/views/`）に index.html を含めるか、生成対象として README に記す。
- **関連**: `view/SKILL.md`、`projects/self/wiki/views/index.html`、`templates/project/wiki/views/README.md`。

---

## SI-008: `/ingest` に「成功基準未達だが部分的シグナルあり」時の確信度の扱い指針がない

- **対象スキル**: `/ingest`
- **状態**: 対応済み（ingest手順4に成功基準の〈支持/反証/判断保留〉3値判定を導入。未達なら原則確信度を上げないルールを追記。2026-07-17）
- **気づいた文脈**: 一巡テストの `DOG-ACT-001`（架空5名）で、成功基準（〈自認〉かつ〈実コスト〉が3名以上）を**満たさなかった**（両立2/5名）。にもかかわらず、行動としての自認が3/5名あったため確信度を上げるか据え置くか判断がぶれ、結局 DOG-H-002 を 3→4 に上げた。これが妥当かはスキルに指針がない。
- **課題**: `/ingest` 手順4は「成功基準に照らして提案」とあるが、**成功基準を満たさなかったが弱いシグナルはある**という中間ケースの確信度操作ルールが無い。安易に上げると架空・部分証拠で確信度が水増しされる（SELF でも架空データ依存として繰り返し問題化している論点）。
- **改善案**:
  1. 「成功基準未達なら原則として確信度を上げない（据え置きか下げる）。上げる場合は理由を成功基準と切り離して明記する」ルールを手順4に追加。
  2. 成功基準を「支持/反証/判断保留」の3値で判定させ、確信度更新はその判定に従わせる。
- **関連**: `ingest/SKILL.md` 手順4、`templates/activity.md` の成功基準、SI-004（架空データ扱い）と地続き。

---

## SI-009: `/new-project` 後に空ディレクトリの `.gitkeep` が残り続ける・掃除手順がない（軽微）

- **対象スキル**: `/new-project`
- **状態**: 対応済み（new-project「守ること」＋grill/plan/ingest/decideの作成手順に「初回レコード作成後 .gitkeep 削除可」を追記。2026-07-17）
- **気づいた文脈**: 一巡テストで `cp -r templates/project/. projects/dogfood/` 後、`hypotheses/` `activities/` `decisions/` に `.gitkeep` が残り、レコードを追加した後も残置された。
- **課題**: 機能上の害はないが、レコードが入ったディレクトリに `.gitkeep` が残るのは不要な残骸。掃除の要否がスキルに書かれていない。
- **改善案**: `/new-project` の「守ること」に「最初のレコード作成後は該当ディレクトリの `.gitkeep` を削除してよい（任意）」を1行添える程度。優先度は低い。
- **関連**: `new-project/SKILL.md` 手順2、`templates/project/wiki/*/.gitkeep`。

---

## SI-010: playbook への参照記法が未定義（`[[playbooks/cpf.md]]` は Obsidian で解決しない）（軽微）

- **対象スキル**: `/decide`（および `templates/decision.md`・`stage.md`）
- **状態**: 対応済み（CLAUDE.md・decide手順1・decision雛形に「schema層(playbooks/・CLAUDE.md)は wikilink でなく相対mdリンクで参照」を明記。2026-07-17）
- **気づいた文脈**: 一巡テストで DEC 本文に移行基準の根拠として `[[playbooks/cpf.md]]` と wikilink を書いたが、playbook は vault 内の接頭辞つきノートではないため Obsidian のグラフ/リンクでは解決しない（stage.md は相対mdリンク `[playbooks/psf.md](../../../playbooks/psf.md)` を使っている）。
- **課題**: playbook を本文から参照する際の記法（wikilink か相対mdリンクか）がスキル/テンプレに明示されておらず、書き手ごとにぶれてリンク切れを生む。
- **改善案**: 「schema層（playbooks/・CLAUDE.md）への参照は wikilink ではなく相対mdリンクで書く」を規約として1行明記する（`/decide`・`stage.md` 雛形）。
- **関連**: `decide/SKILL.md` 手順1、`projects/*/wiki/stage.md`。

---

## SI-011: テストカードの各項目（目的・方法・指標・成功基準）が箇条書きで、見出し構造になっていない

- **対象スキル**: `/plan`（および `templates/activity.md`・`/ingest` のテストカード記入）
- **状態**: 未対応
- **気づいた文脈**: `ai-reskilling` 案件の `/plan` で `AIRE-ACT-002`（発見型インタビュー）のテストカードを作成した際、ユーザーから「目的・方法などは箇条書きではなく**見出しを使ってほしい**」との指摘（2026-07-19）。
- **課題**: 現行 `templates/activity.md` のテストカードは `- **目的**: …`／`- **方法**: …`／`- **指標**: …`／`- **成功基準**: …` の**箇条書き**。項目が長文化すると（方法にスクリプト参照・対象者条件、成功基準に主/副の複数条件、指標に (a)(b)(c) など）1つの箇条書きに情報が詰まり、読みにくく追記しづらい。見出し（`### 目的` 等）にすれば各項目が独立ブロックになり、下に段落・小箇条書き・表を自由に置ける。学習カードは既に `### 事実`／`### 解釈` … と見出し構造なので、テストカードだけ箇条書きなのは不整合でもある。
- **改善案**:
  1. `templates/activity.md` のテストカードを `- **目的**:` の箇条書きから `### 目的`／`### 方法`／`### 指標`／`### 成功基準` の**見出し構造**に変更する（学習カードの `###` と粒度を揃える）。
  2. `plan/SKILL.md` 手順5・`ingest/SKILL.md` のテストカード記述を新形式に合わせる。
  3. 既存の実レコード（テストカードを持つ ACT）は、**成功基準の内容は変えずフォーマットのみ**新形式へ寄せる（検証開始前のカードに限る。後知恵改変・成功基準の書き換えはしない）。
- **関連**: `templates/activity.md`、`plan/SKILL.md` 手順5、`ingest/SKILL.md`、SI-002（スクリプト草稿）と地続き。`projects/ai-reskilling/wiki/activities/AIRE-ACT-002.md` が新形式の適用対象例。

---

## SI-012: 共通規約が9スキルに重複コピーされ、ドリフトの温床になっている

- **対象スキル**: 全スキル（`chabudai`/`decide`/`desk-research`/`formulate`/`ingest`/`lint`/`plan`/`prototype`/`view`）＋`CLAUDE.md`
- **状態**: 対応済み（`CLAUDE.md` に「スキル共通規約」節を新設し、9スキルの `> **パス・ID規約**` プリアンブル・current-project 解決手順・非対話バッチ承認段落を1行参照に畳んだ。2026-07-20）
- **気づいた文脈**: スキル改善の調査（2026-07-20）。「パス・ID規約」ブロックが**9/10スキル**に、current-project 解決手順が**10/10**に、接頭辞/リンク記法規約が**9/10**にほぼ同一文でコピーされていた。
- **課題**: 同じ規約がN箇所に散らばると、1箇所だけ古くなる**ドリフト**が起きる。実際 SI-005（id接頭辞の割れ）も SI-013（下記・リンク深さの誤り）もこれが原因だった。規約変更が9ファイル修正になり、修正しづらい。
- **改善案（実施済み）**: 共通規約（プロジェクト解決・ID/接頭辞・リンク記法と `../` 深さ・.gitkeep・承認規律）を `CLAUDE.md`「スキル共通規約」節に集約し正典化。各スキルは冒頭で1行参照し、固有手順だけを書く。規約変更は1ファイル1修正に。

---

## SI-013: `CLAUDE.md` の playbook 相対リンクの `../` 深さの例が誤解を招く（リンク切れの原因）

- **対象スキル**: `CLAUDE.md`（スキーマ層）／`decide` 他 playbook 参照を書くスキル
- **状態**: 対応済み（SI-012 の「スキル共通規約」節にリンク深さ表を集約。`wiki/` 直下=`../../../`、`wiki/<種別>/` 配下=`../../../../` を明記。2026-07-20）
- **気づいた文脈**: 調査で実レコードの playbook リンクに `../../../`（2件）と `../../../../`（3件）が混在。`CLAUDE.md` の唯一の例は「レコード種別」節（＝サブ階層の H/ACT/DEC）直後にあるのに3階層で、そのまま真似るとリンク切れになる。`decide/SKILL.md` は正しく4階層。
- **課題**: 深さが参照元の位置で変わるのに例が1つだけで、位置による違いが明示されていなかった。
- **改善案（実施済み）**: 深さ表を共通規約に置き、`CLAUDE.md` の旧例文はその表を指すよう修正。

---

## SI-014: `/lint` が既存の決定論ツール `check_testcard_immutable.py` を使っていなかった

- **対象スキル**: `/lint`
- **状態**: 対応済み（lint 手順0に `check_testcard_immutable.py --base origin/main` を追加、項目7を機械検出に格上げ。2026-07-20）
- **気づいた文脈**: 調査で、成功基準の事後書き換え（項目7）が「git履歴で確認できる場合」と手作業任せなのに、専用ツール `tools/check_testcard_immutable.py` と `.githooks/pre-commit`（`--staged`）が既に存在すると判明。
- **課題**: 決定論で検出できるものを LLM の目視に頼っており、取りこぼしとブレの原因だった。
- **改善案（実施済み）**: 手順0で機械実行し、項目7を hwlint と同じく「出力を転記」に。機械が拾えない字面すり替えのみ LLM が補う。

---

## SI-015: `/chabudai` のトリガー語 `yusaburi` がグローバル `/yusaburi` と衝突

- **対象スキル**: `/chabudai`
- **状態**: 対応済み（description から素の「yusaburi」を外し、対象を「仮説検証Wikiのレコード」に限定。汎用の思考揺さぶりはグローバル `/yusaburi` へ誘導する一文を追加。2026-07-20）
- **気づいた文脈**: `chabudai` の description が発火語に「yusaburi」を列挙していたが、別途グローバルスキル `yusaburi`（汎用の思考メンター）が存在し、「yusaburi」だけでどちらが発火すべきか曖昧だった。
- **課題**: 汎用の揺さぶり依頼で Wiki専用の chabudai が誤発火する／その逆。役割境界が曖昧。
- **改善案（実施済み）**: chabudai＝Wikiレコードの反証監査、yusaburi＝アイデア/計画一般、と description で棲み分けを明示。

---

## SI-016: テストカードが箇条書きで学習カード（見出し構造）と不整合（SI-011 の実施）

- **対象スキル**: `/plan`・`/prototype`（および `templates/activity.md`）
- **状態**: 対応済み（`templates/activity.md` のテストカードを `### 目的`／`### 方法`／`### 指標`／`### 成功基準` の見出し構造に変更。plan/prototype の記述も整合。2026-07-20）。**既存 ACT レコードは非改変**（不変ルール6・後知恵バイアス防止／`check_testcard_immutable.py` との干渉回避のため、フォーマット移行は今後の新規カードから）。
- **気づいた文脈**: SI-011（`ai-reskilling` の `/plan` で見出し要望）の残タスク。テンプレのテストカードだけ箇条書きで、学習カードの `### 事実` … と粒度が不揃いだった。
- **改善案（実施済み）**: テンプレとスキルを見出し構造へ。既存レコードは触れず、新規カードから新形式。

---

## SI-017: アクティブプロジェクトの「切り替え」スキルが無い（新規）

- **対象スキル**: `/new-project`（切替モード追加）または新規 `/switch-project`
- **状態**: 未対応
- **気づいた文脈**: 調査（2026-07-20）。`/new-project` は作成＋`current-project` 設定までするが、既存プロジェクト間の切り替えは `projects/current.md` の手編集頼み（self #6 と地続き）。
- **改善案**: 一覧提示 → `current-project` 更新までを1手順にする軽量スキル。優先度中。

---

## SI-018: `/formulate` に重複仮説ガードが無い（新規・軽微）

- **対象スキル**: `/formulate`
- **状態**: 未対応
- **気づいた文脈**: `/desk-research` は「既にカバー済みの仮説と重複しない空白を狙う」と明記するが、`/formulate` 手順2に同種のチェックが無い。
- **改善案**: 手順2で既存 H 群（`index.md`）を読み、重複起票を避けるチェックを1行足す。優先度低。

---

## SI-019: `templates/demo-script.md`（PSFデモ用スクリプト雛形）が未作成（SI-002 残タスク）

- **対象スキル**: `/plan`（demo系スクリプト草稿）／`templates/`
- **状態**: 未対応（SI-002 改善案3の積み残し）
- **気づいた文脈**: `interview-script.md` はあるが demo 用が無く、PSF のデモ検証で毎回スクリプトをゼロから作る。
- **改善案**: `templates/demo-script.md` を新設し、`/plan` の demo 系テストカードから参照させる。

---

## SI-020: `/prototype` が純CPF段階（ソリューション仮説がまだ無い）を想定していない

- **対象スキル**: `/prototype`
- **状態**: 未対応
- **気づいた文脈**: `agent-platform` ステ案件の一巡（2026-07-20）。CPFで `/prototype` を試したが、LPは本質的にソリューション空間の生成物なのに、CPFにはソリューション/買ってもらえる仮説がまだ無い。skill 手順5の stage 規定は「対象仮説のステージが現ステージより**先**なら先取りプレビュー」＝"先のステージの仮説がある"前提で、"同ステージ（CPF）だが生成物がソリューション寄り"のケースを扱っていない。
- **課題**: 今回は自分たち仮説（AGP-H-005・勝ち筋）を対象に先取りプレビュー扱いで回避したが、手順にその扱いの明記がなく、書き手ごとにぶれる。CPFの「ソリューションを見せない」鉄則との緊張も未整理。
- **改善案**: 手順5に「対象がソリューション/買ってもらえる仮説を持たないCPF段階では、(a) 価値提案の反応測定は CPF の問題インタビューと分離した survey ACT として扱い、(b) 本文に先取りプレビュー（現ステージ CPF・ソリューション空間の生成物である旨）を必須明記する」を1項足す。または「CPFでは prototype を推奨しない」と明示してガードする。
- **関連**: `prototype/SKILL.md` 手順5、`playbooks/cpf.md`（ソリューションを見せない鉄則）。実例だった `agent-platform` ステ案件は破棄済み（下記の一巡記録参照）。

---

## SI-021: `/chabudai` の非対話・自動適用パスが実際にはほぼ発火しない（軽微）

- **対象スキル**: `/chabudai`
- **状態**: 未対応
- **気づいた文脈**: 一巡（2026-07-20）で AGP-H-003 を 6→5 に引き下げたが、根拠は「確証バイアス（支持3:反証3を支持だけ計上）」という**解釈判断**だった。chabudai 守ること/共通規約5の自動適用条件「機械的に〈反証〉が確定」にはまず当てはまらない。
- **課題**: 揺さぶりの引き下げは本質的に解釈を伴うため、自動適用パスがほぼ死条文になっている。読み手が「バッチで下げてよい」と誤解する余地。
- **改善案**: chabudai の承認規律を「引き下げは原則すべて対話承認。自動適用は "反証が学習カードで機械的に確定済み" の追認に限る」と現実に寄せて明記。優先度低。

---

## SI-022: `index.md` の架空データ警告バナーが手保守で、生成ビューと二重管理（軽微）

- **対象スキル**: `/ingest`・`/view`（および `index.md` の扱い）
- **状態**: 未対応
- **気づいた文脈**: 一巡（2026-07-20）で `list`/`board` は架空データ警告バナーを generator が自動生成する一方、`index.md` の同種バナーは手更新だった（確信度更新のたびに手で直す）。
- **課題**: 同じ「架空を含む」警告が生成ビューと手保守ファイルに二重にあり、乖離しうる（ドリフト）。
- **改善案**: `index.md` は仮説カタログとして人手保守が正だが、架空警告は「views を見よ」に一本化するか、`/ingest` 手順に「架空ACTを紐づけたら index.md バナーも更新」を明記して同期漏れを防ぐ。優先度低。

---

## 一巡テストの記録（2026-07-20・agent-platform）

`agent-platform`（接頭辞 AGP・テーマ=AIエージェントプラットフォーム）というステ案件を作り、
`/new-project → /desk-research → /formulate → /plan → /prototype → /ingest → /view → /decide → /lint → /chabudai`
を CPF で一巡実行した。desk-research は実Web検索（出典つき）、問題インタビューは架空6名のシミュレーション（確信度上限8・実データ未検証を全レコードに明示）。
全工程で hwlint error 0・testcard-immutable OK・ユニットテスト35件パスを維持。
**SI-012 の共通規約集約リファクタは実地で問題なし**（各スキルの1行参照から規約を迷わず適用でき、SI-013 のリンク深さ表どおり `wiki/activities/` からは `../../../../playbooks/` の4階層で正しく書けた）。
新たな摩擦として SI-020〜022 を抽出。**ステ案件 agent-platform は 2026-07-20 に破棄**（`dogfood` と同じく、改善知見 SI-020〜022 と本記録のみバックログに残す。`self`・`ai-reskilling`・スキーマ層は変更していない）。テーマは複数回ピボットした（AIエージェント基盤→意思決定ログ→Decision Record→仮説検証の一般化）が、これは試行錯誤から使命が事後創発する過程そのものの実演でもあった。

---

## 一巡テストの記録（2026-07-17）

`dogfood`（接頭辞 DOG）という使い捨て案件を作り、`/new-project → /grill → /plan → /ingest → /view → /decide → /lint` を自動で一巡実行して上記 SI-005〜010 を抽出した。テスト案件は改善点抽出後に破棄する（`self` 案件・スキーマ層は変更していない）。各スキルの単体挙動（レコード生成・index/log更新・成功基準の事前確定・架空データ明示・孤立検出）は概ね設計どおり動作した。
